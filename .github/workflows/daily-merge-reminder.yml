name: Daily Merge Reminder

on:
  schedule:
    - cron: '30 4 * * 0'  # runs every day at 10:00 AM IST (4:30 AM UTC)

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send email reminder
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Reminder: Merge feature branch into main"
          to: your.email@example.com
          from: GitHub Actions
          body: |
            Hi 👋,
            Don’t forget to merge your feature branch into main today.
            You can do it manually from the Actions tab.
